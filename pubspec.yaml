name: flutter_app
description: A simple Flutter app with Riverpod state management
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0

flutter:
  uses-material-design: true
  assets:
    - assets/sample.png
